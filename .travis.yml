sudo: required
language: java

jdk:
  - oraclejdk8

env:
  - NODE_VERSION= 6.10.0

install:
  - nvm install $NODE_VERSION

script:
  - nvm use $NODE_VERSION
  - yarn install
  - npm test

cache:
  yarn: true
