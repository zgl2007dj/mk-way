sudo: required
language: java

jdk:
  - oraclejdk8

env:
  - NODE_VERSION= 6.10.0

install:
  - nvm install $NODE_VERSION
  - nvm use $NODE_VERSION
  - sudo npm install -g yarn
  - yarn install

script:
  - npm test

cache:
  yarn: true
